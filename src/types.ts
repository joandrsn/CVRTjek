export type VATQuery = {
  vat: string;
}
export type SearchQuery = {
  search: string;
}
export type cvrapiResponse = {
  slug: string;
  vat: string;
}
export type cvrapiError = {
  error: string;
}